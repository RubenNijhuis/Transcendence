import { LoaderContainer, LoaderLine } from "./Loader.style";

const Loader = (): JSX.Element => {
    return (
        <LoaderContainer>
            <LoaderLine />
        </LoaderContainer>
    );
};

export default Loader;
