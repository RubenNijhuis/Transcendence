import { LoaderContainer, LoaderLine } from "./Loader.style";

const Loader = () => {
    return (
        <LoaderContainer>
            <LoaderLine/>
        </LoaderContainer>
    );
};

export default Loader;
