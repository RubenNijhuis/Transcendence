import { io, Socket } from "socket.io-client";

// Game types
const enum SocketType {
    WaitingRoom,
    Game,
    Chat
}

class CustomSocket {
    // constructor(url: string, type: SocketType) {
    //     this.connection = 
    // }

    emit() {

    }

    on() {
    
    }
}

export default CustomSocket;