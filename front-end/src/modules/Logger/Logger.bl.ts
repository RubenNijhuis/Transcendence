const formatConfig = {
    typeError: (type: string) =>
        `Logger\nType of ${type} was given but doesn't exist in the config`
};

///////////////////////////////////////////////////////////

export { formatConfig };
